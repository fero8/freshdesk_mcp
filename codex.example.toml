# Copy to ~/.codex/config.toml (merge the [mcp_servers.freshdesk-mcp] block).
# Codex requires Streamable HTTP at /mcp — do not use /sse.

[mcp_servers.freshdesk-mcp]
url = "https://mcpfd.68travel.com/sse"
bearer_token_env_var = "FRESHDESK_API_KEY"
enabled = true
