# Freshdesk tenant. API keys are supplied by MCP clients with Authorization: Bearer <api-key>.
FRESHDESK_DOMAIN=yourcompany.freshdesk.com

# Runtime logging
LOG_LEVEL=INFO

# Local MCP clients usually use stdio. Laravel Forge should use sse behind Nginx.
MCP_TRANSPORT=sse
MCP_HOST=127.0.0.1
MCP_PORT=8000

# Optional comma-separated tool controls. Leave blank to expose all tools.
ENABLED_MCP_TOOLS=
DISABLED_MCP_TOOLS=
